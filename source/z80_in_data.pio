.program z80_in_data

start:
;    wait 0 pin 0        ; /IORQ Low
;    wait 0 pin 1        ; /RD Low

    pull block
    mov pindirs, ~null  ; D0-7 OUTPUT
    out pins, 8

    wait 1 pin 1        ; /RD High
    mov pindirs, null  ; D0-7 INPUT
    jmp start
